# CryptoMamba Console — backend deps.
# The frontend is a prebuilt React/Vite bundle in ../web-dist, so nothing here for it.
# pandas/plotly/numpy are needed by the vendored cryptomamba_ui logic and the services.
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pandas>=2.0
numpy>=1.24
plotly>=5.18
httpx>=0.27
requests>=2.31
